{
  "name": "render-platform",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run install:all && npm run build --prefix frontend",
    "migrate": "npm install --prefix backend && npm run migrate --prefix backend",
    "seed:usuarios": "npm run seed:usuarios --prefix backend",
    "start": "npm start --prefix backend",
    "start:hostinger": "node backend/src/server.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:hosting": "bash scripts/build-hosting.sh"
  }
}
