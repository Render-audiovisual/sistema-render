node_modules/
dist/
/backend/backups/
.env
.env.*
!.env.example
# El Client ID de Google no es secreto (viaja al navegador de todas
# formas) — se commitea porque Vite solo lee .env.production en el
# build, y el hosting de Hostinger no inyecta env vars en esa etapa.
!frontend/.env.production
.DS_Store
*.zip

# Generados por scripts/build-hosting.sh (copia estática del frontend en
# la raíz, lista para subir a un hosting aparte del backend). Se
# regeneran con `npm run build:hosting`, no se versionan.
/index.html
/assets/
/favicon.png
/apple-touch-icon.png
/.htaccess
